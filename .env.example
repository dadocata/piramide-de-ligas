# Puerto del servidor (default: 3000)
PORT=3000

# Ruta del archivo SQLite (default: data.db en la raíz del proyecto)
DB_PATH=data.db

# Duración de sesión en días (default: 30)
SESSIONS_DAYS=30

# Si se define, el registro queda restringido a quien ingrese este código
# REGISTER_CODE=amigos2026